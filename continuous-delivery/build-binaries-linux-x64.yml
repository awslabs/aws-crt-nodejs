version: 0.2
phases:
  build:
    commands:
      - mkdir linux-x64
      - cd aws-crt-nodejs
      - builder build --project=.
      - cp -r dist/bin/linux-x64/* ../linux-x64

artifacts:
  files:
    - 'linux-x64/**/*'
