version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - apt-get update
      - apt-get install cmake3
  build:
    commands:
      - cd aws-crt-nodejs
      - bash ./codebuild/build-binaries-unix.sh

artifacts:
  files:
    - 'dist/bin/*'
