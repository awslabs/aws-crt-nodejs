version: 0.2
phases:
  build:
    commands:
      - mkdir linux-x64
      - cd aws-crt-nodejs
      - builder --version=v0.6.0 build --project=aws-crt-nodejs run_tests=false
      - cp -r /root/aws-crt-nodejs/dist/bin/native/* ../linux-x64/

artifacts:
  files:
    - 'linux-x64/**/*'
